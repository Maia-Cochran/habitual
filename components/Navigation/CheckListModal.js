import React from 'react';
import { Text, StyleSheet } from 'react-native';

const CheckListModal = () => {
    return <Text style={styles.textStyle}>CheckList</Text>
}

const styles = StyleSheet.create({
    textStyle: {
        fontSize: 30
    }
})

export default CheckListModal